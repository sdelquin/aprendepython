def calc(expression: str) -> int | float:
    """Si el operador no es suma, resta, multiplicación o división,
    se tendrá que elevar una excepción de tipo ValueError con el
    mensaje:
    Operator <operator> is not supported!
    """
    pass
